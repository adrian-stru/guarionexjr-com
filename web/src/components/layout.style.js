import styled from 'styled-components'
import {config} from '../styles'

const StyledLayout = styled.div`
  width: 100%;
  height: 100%;
  padding: 6px ${config.sitePaddingX}px;
`

export {StyledLayout}
