import styled from 'styled-components'

const Wrapper = styled.article`
  overflow: hidden;
  margin-bottom: 50px;
`

const StyledTitle = styled.h2`
  font-size: 18px;
  font-weight: 400;
  margin-bottom: 10px;
`

const TextWrapper = styled.div`
  margin-bottom: 30px;
`

export {Wrapper, StyledTitle, TextWrapper}
